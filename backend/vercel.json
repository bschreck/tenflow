{
  "installCommand": "python3 -m pip install uv && python3 -m uv sync",
  "buildCommand": "echo 'Build complete'",
  "startCommand": "python3 -m uv run uvicorn tenflow.main:app --host 0.0.0.0 --port $PORT"
}