{
  "installCommand": "pip install uv && uv sync",
  "buildCommand": "uv run uvicorn tenflow.main:app --host 0.0.0.0 --port 8000",
  "devCommand": "uv run uvicorn tenflow.main:app --reload --host 0.0.0.0 --port 8000",
  "outputDirectory": "src",
  "env": {
    "PYTHONPATH": "src"
  }
}